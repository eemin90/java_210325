package p06.lecture.p8access_modifier;

public class A04Constructor {
	// 클래스의 접근 제한자는 public, default(package private)만 가능
	
	// 생성자를 만들지 않으면 자동으로 기본 생성자(매개 변수가 없는 생성자)가 추가되는데
	// 이 때 기본 생성자의 접근 제한자(접근 지정자)는 class의 접근 제한자가 붙는다.
	
}
